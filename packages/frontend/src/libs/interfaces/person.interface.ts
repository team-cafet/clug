export interface IPerson {
  email: string;
  firstname?: string;
  lastname?: string;
  birthdate: string;
  pictureURL?: string;
  phone?: string,
  street?: string,
  city?: string,
  streetNumber?: number,
  postalCode?: number,
}
