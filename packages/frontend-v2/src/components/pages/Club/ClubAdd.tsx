import React from 'react';
import { ClubForm } from '../../organisms/ClubForm';

interface IProps {}

export const ClubAdd = (props: IProps) => {
  // useEffect(() => {

  // }, []);

  return (
    <>
      <ClubForm />
    </>
  );
};
